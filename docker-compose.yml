version: "3.9"

services:
  web:
    build: .
    container_name: website
    ports:
      - "8080:80"

    volumes:
      - website-data:/usr/share/nginx/html

    networks:
      - app-network

volumes:
  website-data:

networks:
  app-network:
